# -*- mode:python -*-
Import('*')

# gEMA SOURCES
PySource('gEMA', '__init__.py') # gEMA Module init
PySource('gEMA', '__main__.py') # gEMA main
PySource('gEMA', 'config.py')
PySource('gEMA', 'options.py')
PySource('gEMA', 'manager.py')
PySource('gEMA', 'rpc.py')
PySource('gEMA', 'rpc_data.py')

